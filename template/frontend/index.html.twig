{% extends "base.html.twig" %}

{% block title %}FRONTEND !{% endblock %}

{% block body %}
    
    <h3>Dernier Posts :</h3>
    <hr />
    {% for Post in LastPostList %}
    <p>{{ Post.title }} - {{ Post.dateCreate|format_datetime('short', 'none', locale='fr') }}</p><br />
    <p>{{ Post.chapo }}</p>
    <hr />
    {% endfor %}
    <p>Bonjour {{ name }}</p>
{% endblock %}